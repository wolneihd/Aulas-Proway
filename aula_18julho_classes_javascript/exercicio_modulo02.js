const { nomeCompleto, validarIdade, aumentoSalarial, faturamentoBruto } = require('./exercicio_modulo01')

// Exercicio 01:
nomeCompleto('Pedro', 'Henrique');

// Exercicio 02:
validarIdade(5);
validarIdade(18);
validarIdade(21);

// Exericio 03 (informar segunda variável como %):
aumentoSalarial(2500, 15);

// Exericio 04 (valor_produto, qtd_vendass):
faturamentoBruto(1.75, 150)